apiVersion: v1
kind: Service
metadata:
  name: my-ag-ui-app-service
  labels:
    app: my-ag-ui-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: my-ag-ui-app