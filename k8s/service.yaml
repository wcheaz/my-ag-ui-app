apiVersion: v1
kind: Service
metadata:
  name: my-ag-ui-app-service
  namespace: default
  labels:
    app: my-ag-ui-app
spec:
  type: ClusterIP
  selector:
    app: my-ag-ui-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    name: http
